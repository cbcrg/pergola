general:
  branches:
    ignore:
      - gh-pages
    only:
      - master

machine:
  python:
    version: 2.7.9

dependencies:
  pre:
    - pip install -r requirements.txt
    - pip install scipy
    - cd pergola
    - python setup.py install

test:
#  pre:
#    - pip install scipy
#    - cd pergola
#    - python setup.py install
  override:
    - nosetests pergola/test/test_all.py


